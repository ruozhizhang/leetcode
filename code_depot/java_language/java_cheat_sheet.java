// Array

// String
  str.length() -> int
  String.join(delimeter, CharSeq or itetable of Char); -> String
  str.split(regex, limit); -> Array of Strings
  str.toUpperCase(); -> String

// List

// HashMap

// HashSet
